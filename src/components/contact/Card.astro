---
const { title, subtitle, data } = Astro.props;
---

<div class="flex flex-col gap-1">
  <slot />
  <p class="text-black font-semibold text-xl">{title}</p>
  <p class="text-paragraph-800 font-medium text-base">{subtitle}</p>
  <p class="text-paragraph-800 font-bold tracking-wider text-sm">{data}</p>
</div>
